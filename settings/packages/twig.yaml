twig:
    file_name_pattern: '*.twig'
    default_path: '%kernel.project_dir%/interfaces'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
when@test:
    twig:
        strict_variables: true